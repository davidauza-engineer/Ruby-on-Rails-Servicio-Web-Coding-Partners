<%= form_with(model: tecnologia, local: true) do |form| %>
  <% if tecnologia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tecnologia.errors.count, "error") %> prohibited this tecnologia from being saved:</h2>

      <ul>
        <% tecnologia.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
